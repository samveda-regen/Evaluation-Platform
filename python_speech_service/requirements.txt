fastapi==0.115.6
uvicorn[standard]==0.34.0
faster-whisper==1.1.0
requests==2.32.3
python-multipart==0.0.20
pydantic==2.10.3

# Pronunciation scoring (optional — app.py degrades gracefully if these fail to
# load/import, e.g. espeak-ng missing on this machine). CPU-only torch wheel;
# swap the index for a CUDA build if deploying with a GPU.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.13.0
transformers==5.15.0
phonemizer==3.4.0
